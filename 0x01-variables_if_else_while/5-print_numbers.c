#include <stdio.h>

/**
 * main - Entry point
 * author: opiro
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int opiro;

	for (opiro = 0; opiro < 10; opiro++)
	{
		printf("%d", opiro);
	}
	printf("\n");
	return (0);
}
